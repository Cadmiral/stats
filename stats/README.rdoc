== README

Create Job to do: 

run testTables.rb (or main.rb?)
run create_table.rb
run get_salaries.rb (add to create_table.rb script)
run in DB :

UPDATE todays_game SET dollar_per_point = round((salary/avg_fd_points),2);

_____________




Please feel free to use a different markup language if you do not plan to run
<tt>rake doc:app</tt>.
